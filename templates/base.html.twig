<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="description" content="Jeu de memory avec des pokemon">
		<meta name="author" content="Hugo">

        <title>{% block title %}{% endblock %} - Pokemon MemoryGame</title>

        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.0/css/all.min.css?version=#">

        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>

    <body id="body">
        <header>
            {# MENU #}
            <nav>
                <a href="{{ path('app_home') }}">Accueil</a>

                {# Affichage des boutons connexion et déconnexion si un utilisateur est connecté ou non. #}
                {% if(not app.user) %}
                    <a href="{{ path('app_login') }}">Connexion</a>
                {% else %}
                    <a href="{{ path('app_logout') }}" onclick="return window.confirm('Confirmer la déconnexion ?')">Déconnexion</a>
                {% endif %}
            </nav>
        </header>

        {% block body %}{% endblock %}
        
        <footer>

        </footer>
    </body>
</html>
